# 备班补链修复任务计划

- [ ] Task 1：pass=0 将 `补` 加入白班跳过条件
    - 1.1：定位 pass2Standby 内 pass=0 循环中白班候选过滤行（约第 192 行）
    - 1.2：将 `['休', '上半']` 改为 `['休', '上半', '补']`，使 `补` 天不被优先选作备班

- [ ] Task 2：备班赋值时仅在 `白` 类型触发次日补休与上半转换
    - 2.1：将 `_setAMRest` 调用和其他白班转 `上半` 的循环，整体包裹在 `if (cell.type === '白')` 条件内
    - 2.2：确保 `补` / `上半` 类型备班时，不产生次日 `补` 也不转换他人
    - 2.3：`consecBefore` 累计和 pointer 推进逻辑保持不变（块状分配逻辑不受影响）
