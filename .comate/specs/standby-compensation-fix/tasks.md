# 备班补偿逻辑根本性修复任务计划

- [ ] Task 1：修复 `_setAMRest` 跳过条件，允许 `休(补)` 覆盖 `上半`
    - 1.1：将跳过列表从 `['休', '下', '上半', '休(补)']` 改为 `['休', '下', '休(补)']`
    - 1.2：更新注释说明：`休(补)` 会覆盖 `上半`，因为上午补休优先于上午上班

- [ ] Task 2：简化 `pass2Standby` 赋值区块，移除块状分配机制
    - 2.1：`白` 类型备班改为立即调用 `_setAMRest`（不再受 `consecBefore + 1 >= maxConsecutive` 条件约束）
    - 2.2：移除指针「停留」逻辑（`consecBefore + 1 >= maxConsecutive` 才推进的条件），恢复为每次白班备班后始终推进指针
    - 2.3：保留 `休(补)` 类型兜底分支（`cell.type === '休(补)'` 时调用 `_setAMRest`）
    - 2.4：保留其他白班人员转 `上半` 的逻辑（`白` 类型备班当天执行）
