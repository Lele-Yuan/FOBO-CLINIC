# 新增「休(补)」班次类型并修复备班块时序 — 完成总结

## 修改内容

### Task 1 — 新增 `休(补)` 显示定义

- **`SHIFT_COLORS`**：新增 `'休(补)': { bg: '#E8ECFF', text: '#7B87C8' }`
- **`LEGEND_LIST`**：新增条目「备班后全天补偿休息，计 0.5 天」
- **`getCellDisplayBg`**：新增 `'休(补)'` 返回 `#E8ECFF`

### Task 2 — `_setAMRest` 改为生成 `休(补)`

```diff
- next.type = '补'
+ next.type = '休(补)'
  // 跳过条件同时加入 '休(补)'，防止重复覆盖
```

### Task 3 — `pass2Standby` 块时序与兜底分支

**pass=0 排除列表**：
```diff
- ['休', '上半', '补']
+ ['休', '上半', '补', '休(补)']
```

**赋值区块核心逻辑**：
```
白 类型：
  - 每块日：其他白班 → 上半
  - 仅块完成时：调用 _setAMRest → 次日生成 休(补)
休(补) 类型（兜底）：
  - 无条件调用 _setAMRest → 次日生成 休(补)  ← 解决麦苗周日问题
上半 类型：无副作用
```

### Task 4 — `_calcRestScore` 积分规则

| 类型 | T_week 积分 |
|---|---|
| `休` / `下` | 1.0 |
| `上半` / `下半` | 0.5 |
| `休(补)` | **0.5**（新增）|
| `补` | 0（手动排班专用）|

---

## 预期效果（4 人倒班，maxConsecutive=2，T_week=1.5）

| 日 | 麦苗 | 煮蛋 | 积分（麦/煮）|
|---|---|---|---|
| 二 | 白+备（块1）| 上半 | 0 / 0.5 |
| 三 | 白+备（块2）| 上半 | 0 / 0.5 |
| 四 | 休(补) | 白+备（块1）| 0.5 / 0 |
| 五 | 白 | 白+备（块2）| 0 / 0 |
| 六 | 休(pass4) | 休(补) | 1.0 / 0.5 |
| 日 | — | — | — |
| **合计** | | | **1.5 ✓ / 1.5 ✓** |

麦苗周六备班 → 周日生成 `休(补)` ✓（`休(补)` 兜底分支恢复了 `_setAMRest`）
