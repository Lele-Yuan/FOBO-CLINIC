# 新增「休(补)」班次类型并修复备班块时序任务计划

- [ ] Task 1：新增 `休(补)` 班次类型的显示定义
    - 1.1：在 `SHIFT_COLORS` 中添加 `'休(补)'` 颜色定义（bg: `#E8ECFF`，text: `#7B87C8`）
    - 1.2：在 `LEGEND_LIST` 中添加 `'休(补)'` 图例条目（描述：备班后全天补偿休息，计 0.5 天）
    - 1.3：在 `getCellDisplayBg` 中添加 `'休(补)'` 返回其 bg 颜色

- [ ] Task 2：`_setAMRest` 改为生成 `'休(补)'`
    - 2.1：将 `next.type = '补'` 改为 `next.type = '休(补)'`
    - 2.2：将跳过条件中的 `'上半'` 保留，同时将 `'休(补)'` 加入跳过列表（已是休(补)则不覆盖）
    - 2.3：更新注释，说明生成 `休(补)` 而非 `补`

- [ ] Task 3：修复 `pass2Standby` 块时序与兜底分支
    - 3.1：pass=0 排除列表加入 `'休(补)'`（与 `'休'`、`'上半'`、`'补'` 并列）
    - 3.2：`白` 类型备班：`上半` 转换保持每块日执行；`_setAMRest` 改为仅在块完成时触发（`consecBefore + 1 >= maxConsecutive`）
    - 3.3：新增 `'休(补)'` 兜底分支：`cell.type === '休(补)'` 时无条件调用 `_setAMRest`

- [ ] Task 4：`_calcRestScore` 将 `休(补)` 计入 0.5 天
    - 4.1：添加 `if (t === '休(补)') score += 0.5`
    - 4.2：更新注释说明各班次积分规则
